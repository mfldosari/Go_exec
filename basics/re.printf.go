package main

import (
	"fmt"
)

func main() {
	var name string = "moha"
	age := 22
	var maried bool = false

	fmt.Printf("the name is %v, and he is %v years old, is he maried? %v", name, age, maried)
}
